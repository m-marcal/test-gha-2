on:
  workflow_dispatch:

jobs:
    hello_world_job:
        runs-on: ubuntu-latest
        name: A job to say hello
        steps:
          - uses: m-marcal/test-gha@main
            with:
              env: 'TEST'